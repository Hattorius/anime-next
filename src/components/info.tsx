import styled from "styled-components";

const Background = styled.div`
    background: #000;
    color: #fff;
    padding: 1rem;
    min-height: 80vh;
`;

export default function Info() {
    return (
        <Background>
            
        </Background>
    );
}
